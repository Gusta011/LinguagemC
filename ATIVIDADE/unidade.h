#include <stdio.h>
#include <stdlib.h>

typedef struct dados_unidade
{
    char nomeunidade[100];
    char localizacao[100];
    char horariofuncionamento[100];
    char telefonecontato[50];

}unidade;